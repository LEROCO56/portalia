# Backlog Portalia — fuente única de pendientes

> Lo lee y actualiza la tarea programada "Portalia — loop de pendientes" cada hora.
> Regla: **nada local, todo en GitHub**. Cada avance se hace commit + push a `LEROCO56/portalia` (rama `main`).
> Estados: `[ ]` pendiente · `[~]` en curso · `[x]` hecho · `[!]` bloqueado (con motivo y qué necesita Leo).

## 0. Repositorio
- [x] Repo creado en GitHub y `main` sincronizado (commit 266871b).
- [x] Verificar que `npm install && npm run build && npm run typecheck` pasen; corregir errores. (verificado 2026-09-23: los 3 terminan en 0, sin errores)

## 1. Infraestructura (docs/DEPLOY.md)
- [x] Crear proyecto Supabase `portalia` y aplicar `supabase/migrations/001_initial.sql`. (ref `rgmdymzxapkjkomfllok`, ACTIVE_HEALTHY, región us-east-2 en vez de São Paulo; 6 tablas con RLS: profiles, course_progress, threads, replies, audits, affiliates)
- [!] Configurar Auth (magic link, Site URL y Redirect URLs). Bloqueado: el MCP de Supabase no expone la configuración de Auth y el dashboard pide inicio de sesión de Leo. Necesita (2 min): supabase.com/dashboard/project/rgmdymzxapkjkomfllok/auth/url-configuration → Site URL `https://portalia-m0f.pages.dev` y Redirect URL `https://portalia-m0f.pages.dev/api/auth/callback` (luego sumar las de portalia.com.co cuando el dominio apunte).
- [x] Conectar repo a Cloudflare Pages, cargar variables de entorno y primer deploy. (https://portalia-m0f.pages.dev responde 200)
- [x] Corregido 2026-09-23: en producción **no había variables de Supabase** (Pages ignora las variables de texto del dashboard cuando existe `wrangler.toml`; solo estaba el secreto `HOTMART_WEBHOOK_SECRET`). Efecto: login, panel, foro y webhook no tocaban Supabase y el webhook aceptaba peticiones sin firma. Arreglo: `[vars]` públicas en `wrangler.toml` + lectura de env en tiempo de ejecución (`src/lib/env.ts`) + webhook rechaza todo si falta el secreto. Verificado en pages.dev: foro lee hilos reales, login llega a Supabase Auth, webhook sin hottok → 401.
- [!] Cargar el secreto `SUPABASE_SERVICE_ROLE_KEY`. Bloqueado: el MCP de Supabase solo entrega llaves públicas, la service role solo se ve en el dashboard con sesión de Leo. **Sin esto el webhook de Hotmart no puede asignar `role='buyer'` a quien compra.** Necesita (2 min): supabase.com/dashboard/project/rgmdymzxapkjkomfllok/settings/api-keys → copiar `service_role` → Cloudflare → Pages → portalia → Settings → Variables and secrets → Add → tipo Secret, nombre `SUPABASE_SERVICE_ROLE_KEY` → Save → Deployments → Retry del último.
- [!] DNS en GoDaddy (hoy portalia.com.co resuelve a IPs de parqueo de GoDaddy 3.33.130.190/15.197.148.33, sin respuesta HTTPS; GoDaddy no admite CNAME en `@`, así que el camino es agregar `portalia.com.co` como dominio personalizado en Pages y pasar los nameservers a Cloudflare, o CNAME solo en `www` + reenvío de `@`). Bloqueado: pasar los nameservers de portalia.com.co a Cloudflare cambia toda la zona DNS (incluido correo, si lo hay) y agregar el dominio personalizado en Pages requiere el dashboard de Cloudflare con sesión de Leo; es decisión suya. Necesita (10 min): Cloudflare → Pages → proyecto portalia → Custom domains → `portalia.com.co` y seguir el asistente (le pedirá cambiar los NS en GoDaddy por los de Cloudflare). Después: agregar las Redirect URLs de portalia.com.co en Supabase y cambiar la URL del webhook en Hotmart.
- [x] Webhook Hotmart registrado (4 eventos) con `HOTMART_WEBHOOK_SECRET` = hottok real. Endpoint verificado: POST sin hottok → 400 (rechaza). Cambiar la URL a portalia.com.co cuando el DNS quede.
- [x] Verificación en producción (pages.dev): /, /auditor, /login, /sitemap.xml, /robots.txt, /metodo, /precios, /blog → 200; /curso y /panel → 302 a login (correcto).

## 2. Contenido del curso y ventas
- [x] Lecciones 02 a 08 en `content/lecciones/` (guion + resumen + ejercicio), siguiendo el método CITAR. (02 C · 03 I · 04 T · 05 A · 06 R · 07 plan 30 días · 08 caso Serenity)
- [x] Completar secuencia de 7 correos en `content/email/`. (reescrita: arco resultado → 3 arreglos → oferta → objeciones → cierre; firma "El equipo Portalia"; sin urgencia falsa; UTM y notas de cumplimiento Ley 1581/1480)
- [x] Casos reales en `content/casos/` (Serenity Spa como caso 1). (`01-serenity-spa.md`, alineado con /casos/serenity)
- [x] Biblioteca de prompts en `content/prompts/`. (`biblioteca-citar.md`: 20 prompts por paso C-I-T-A-R)
- [x] 4 artículos de blog SEO/AEO en `content/blog/`. (medir visibilidad en IA, aparecer en Perplexity, llms.txt, Google Business Profile)
- [x] Calendario editorial 30 días (LinkedIn, X/Threads, TikTok/Reels) en `docs/CALENDARIO.md`.
- [x] Publicar los 4 artículos de `content/blog/` como páginas en `src/pages/blog/` (con Article + FAQPage JSON-LD), enlazarlos en `/blog` y en `sitemap.xml`. (verificado en pages.dev: 4 × 200, FAQPage presente, sitemap con 28 URLs incluidas 3 que faltaban)
- [x] Secuencia nurture de 7 correos automatizada: migración 004 (inscripción de `role='free'`, salida al comprar, baja por token, `pg_cron` cada hora), Edge Function `nurture-send` (plantillas generadas desde `content/email/nurture-secuencia.md`, UTM, `src` de Hotmart, idempotencia, List-Unsubscribe) y página `/email/baja`. Verificado: cron llama la función → 200 `configured:false`; secreto falso → 401; baja con token falso → false; build y typecheck en 0.
- [!] Activar el envío real de los correos nurture. Bloqueado: no hay cuenta ni API key de Resend, y verificar el remitente `@portalia.com.co` exige registros DNS (depende del bloqueo de DNS de la sección 1). Necesita (10 min, ver docs/DEPLOY.md §9): crear cuenta en resend.com, agregar dominio, crear API key y cargar en Supabase → Edge Functions → Secrets `RESEND_API_KEY` y `NURTURE_FROM`.

## 3. Fase 2 del producto
- [x] Auditor Pro real: `/api/auditor` lee la web en vivo (página, robots.txt con detección de bots de IA bloqueados, sitemap.xml, llms.txt, JSON-LD Organization/LocalBusiness/FAQPage/sameAs, preguntas en encabezados, título, meta, H1, canonical/OG) y calcula el puntaje por pasos CITAR (`src/lib/aeo-audit.ts`). Invitados: diagnóstico real sin guardar; free: 3 cada 24 h con historial; miembros: ilimitado + resumen y plan de 30 días con Claude. Protección SSRF. El formulario público `/auditor` ya usa el endpoint (respaldo heurístico si falla). (verificado en producción: portalia-m0f.pages.dev → 92/100 tras llms.txt + FAQPage, IP privada → 400)
- [!] Activar la parte de Claude del Auditor Pro. Bloqueado: falta la credencial. Necesita (3 min): crear una API key en console.anthropic.com y cargarla en Cloudflare → Pages → portalia → Settings → Variables como secreto `ANTHROPIC_API_KEY` (opcional `ANTHROPIC_MODEL`, por defecto `claude-sonnet-4-5`), luego re-desplegar. Sin la key el auditor funciona igual, solo sin el plan redactado por IA.
- [x] Realtime en foro de comunidad: páginas `/comunidad/[id]` (hilo + respuestas en vivo, JSON-LD DiscussionForumPosting) y `/comunidad/nuevo` (antes eran enlaces rotos), aviso en vivo de hilos nuevos en `/comunidad`. Migraciones `002_comunidad_realtime.sql` y `003_endurecer_funciones.sql` aplicadas en Supabase: solo miembros publican (RLS con `is_member()`), funciones de trigger sin EXECUTE por RPC, `author_name` por trigger para no exponer emails, `threads` y `replies` en la publicación `supabase_realtime`.
- [x] `/curso` entregable hoy: `/curso/<lección>` muestra la guía escrita completa de las 8 lecciones (desde `content/lecciones/`), progreso real en `course_progress` (marcar/desmarcar, barra de avance, "seguir con la lección N") y acceso para buyer/affiliate/admin (igual que la comunidad). Soporte de video firmado de Bunny Stream listo (`src/lib/course.ts`, token SHA-256 con expiración de 4 h): aparece solo cuando existan `BUNNY_*`.
- [!] Videos del curso en Bunny Stream. Bloqueado: los videos no están grabados y no hay cuenta/librería de Bunny. Necesita: grabar las 8 lecciones (los guiones están en `content/lecciones/`), subirlas y cargar `BUNNY_LIBRARY_ID`, `BUNNY_VIDEO_IDS` y el secreto `BUNNY_TOKEN_KEY` (docs/DEPLOY.md §10).

## 4. Brechas detectadas por el propio auditor (portalia-m0f.pages.dev = 68/100)
- [x] Publicar `/llms.txt` con resumen de Portalia y páginas clave.
- [x] Home: sección de 7 preguntas frecuentes visible + FAQPage JSON-LD + H2/H3 en forma de pregunta; `/metodo` también emite FAQPage de su FAQ existente.
- [!] `sameAs` en el Organization de la home. Bloqueado: no hay perfiles oficiales de Portalia registrados en el repo (Instagram, LinkedIn, TikTok, Google Business Profile). Necesita: que Leo pase las URLs de los perfiles creados, o confirmar que se creen; se agregan en `src/pages/index.astro`.

## Bitácora
- 2026-09-23: backlog creado a partir del documento "Portalia — Ecosistema Fase 2"; se eliminó `.git/index.lock` huérfano que bloqueaba commits.
- 2026-09-23 03:16 (Bogotá): loop horario. Verificados build/typecheck, Supabase, Pages, webhook y rutas en producción (pages.dev) → marcados [x]. Escritas lecciones 02–08 completas. Auth de Supabase marcado [!] (requiere login de Leo). La copia local C:\Dev\EBOOCK no estaba conectada a esta sesión y el proxy de git de la nube no tiene credencial para el repo: los cambios se subieron por la interfaz web de GitHub (sesión de Chrome).
- 2026-09-23 03:45 (Bogotá): secuencia de 7 correos reescrita; caso Serenity, biblioteca de 20 prompts, 4 artículos de blog y calendario de 30 días creados. Agregadas 2 brechas nuevas (publicar blog en el sitio, cargar correos en Resend). Subido por la web de GitHub.
- 2026-09-23 04:05 (Bogotá): publicados los 4 artículos como páginas del blog + sitemap corregido (deploy verificado en pages.dev). DNS marcado [!] (requiere decisión y sesión de Leo en Cloudflare/GoDaddy). Pendientes ejecutables restantes: Auditor Pro con API de Claude, realtime del foro, carga de correos en Resend.
- 2026-09-23 04:17–04:50 (Bogotá): Auditor Pro real (señales medidas + plan con Claude para miembros), foro con Realtime y páginas de hilo/nuevo hilo, migración 002 aplicada, `/llms.txt` publicado. Build y typecheck en 0. Carpeta local no conectada y proxy git sin credencial: subido por la web de GitHub. Hallazgo crítico corregido: producción corría sin variables de Supabase (ver sección 1); falta el secreto service role ([!]). FAQPage en home y /metodo. Migración 003 (advisor de seguridad). Pendiente ejecutable: correos nurture (Resend o cron).
- 2026-09-23 06:10–06:40 (Bogotá): cerrado el nurture: la función `nurture-send` ya estaba desplegada en Supabase pero su código no estaba en el repo → versionada en `supabase/functions/nurture-send/` + página `/email/baja` (Ley 1581, one-click). Envío real marcado [!] (falta Resend + DNS). `/curso` pasó de "Próximamente" a curso escrito completo con progreso y soporte de video Bunny firmado; videos [!]. DEPLOY.md §9 y §10. Build y typecheck en 0. Carpeta local no conectada y proxy git sin credencial: subido por la web de GitHub.
